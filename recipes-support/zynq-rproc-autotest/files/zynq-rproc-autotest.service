[Unit]
Description=Run zynq-rproc ptest at boot, print to console, power off
# Run once the system is fully up (equivalent to the old sysvinit "start 99 5").
After=multi-user.target

[Service]
Type=oneshot
ExecStart=/usr/libexec/zynq-rproc-autotest

[Install]
WantedBy=multi-user.target
